# 本地自定义功能记录

> 状态：已合入当前 `sprite-video-lab` / `XSXB-Frame-Tuner` 工作副本（2026-08-29）。

## sprite-video-lab

涉及文件：`server.py`、`app/index.html`、`app/app.js`、`app/styles.css`

### 1. 缩放变体 x2 / x4

**做什么**：缩放处理可选 x2、x4，带预览卡，可导出 Frames / Sheet / MOV / GIF。

**怎么做**：
- `server.py` 的 `MAGIC_VARIANTS` 增加 `{ key: "x4", scale: 4.0, dir: "frames-x4" }`、`{ key: "x2", scale: 2.0, dir: "frames-x2" }`
- 前端变体列表、预览卡、导出按钮（如 `exportMagicX2/X4FramesButton`）与 `data-variant-key` 全链路绑定

### 2. 运行日志 Console + 停止任务

**做什么**：点底部状态栏打开日志面板；显示任务/阶段/进度/日志；可停止当前任务（当前帧结束后中断）。

**怎么做**：
- 后端线程安全状态：`running / cancel_requested / task / stage / current / total / lines`
- API：`GET /api/runtime-console`、`POST /api/runtime-console/cancel`
- `TaskCancelled`；长任务循环：`begin → progress(+ check_cancelled) → end`
- 批处理 / 抠图 / 缩放循环内写进度与日志
- 前端：状态栏开面板、轮询、停止按钮调 cancel；配套 CSS

### 3. 导出文件夹按素材名

**做什么**：`channel_wndup.mp4` → 导出目录 `channel_wndup`（不用时间戳）。

**怎么做**：
- `export_basename_from_source_name`：从 `display_name` / `source_path` 取 stem
- `allocate_export_directory`：同名且已是本工具导出目录（含 `export.json` 或 `frames/`）则覆盖，否则 `name-2` 避让

### 4. 预览按原画布像素比例对比

**做什么**：x2/x4 与原版在对比区按源画布像素统一显示尺度，避免各自 fit 后视觉同大。

**怎么做**：前端对比预览按源画布像素统一显示比例（`app.js` + 样式）。

### 5. 批量裁图与批量不透明度

**做什么**：在「帧检查与导出」对选中帧做可预览的批量编辑，再写回当前查看源（未处理原帧或某一缩放变体）。

**裁图（三种）**：
- 透明包围盒：选中帧 alpha 并集 bbox + padding（默认 24）
- 固定边距：上/右/下/左像素
- 预览拖框：在预览画布拖矩形，同一框套到所有选中帧

**不透明度（两种）**：
- 整体倍率 0–100% 乘到 alpha
- 阈值：区间内 alpha 清零或拉满

**羽化遮罩**：
- 形状：椭圆 / 圆 / 矩形 / 套索
- 羽化宽度 + 可反相；形状内保留、边缘软化、外侧透明
- 操作顺序：裁图 → 不透明度 → 羽化

**怎么做**：
- UI：`details#batchEditPanel`（裁图/不透明度/羽化、实时预览、应用、撤销）
- 前端：`paintFrameOnCanvas` 实时套用；拖框/拖形状坐标映射到源图像素
- 后端：`POST /api/batch-edit`、`POST /api/batch-edit/undo`；羽化用硬边遮罩 + GaussianBlur 乘到 alpha
- 应用前快照到 `work/jobs/<id>/batch_edit_snapshot/`（仅保留最近一次）；编辑原帧会 bump `content_token` 并使缩放缓存失效

---

## XSXB-Frame-Tuner

涉及文件：`tools/animation_tuner/public/index.html`、`tools/animation_tuner/public/app.js`（Lite / 完整版共用）

### 1. 调整区数字可键盘直接输入

**做什么**：缩放 / 旋转 / 缩放 X·Y / 偏移 X·Y 可点选输入、粘贴数字。

**怎么做**：
- `#baseScale` `#baseRotation` `#baseScaleX` `#baseScaleY` `#baseX` `#baseY` 去掉 `readonly`
- `adjustmentNumberInputs()` 的 keydown **不要**对普通字符 `preventDefault`；保留 ↑↓ / PageUp·Down 步进

### 2. QWER 画布变换（取代 R/Z+滚轮）

**做什么**：Q 选择、W 移动、E 旋转、R 缩放；主精灵也可以在画布上拖。空画布仍平移。滚轮只缩放视图。

**怎么做**：
- 工具栏 Q/W/E/R 与快捷键同步，输入框不抢键
- `drawTransformGizmo` / `hitTestTransformGizmo`：移动红绿轴、旋转圈、缩放中心块/轴向块
- 主精灵写入当前 Character/Group/Frame Base；附加图仍要点中图像才选中
- 拖尾工作区或 Alt 改框优先于 gizmo
- 画布、胶片条、主界面禁用浏览器右键菜单（输入框保留）

### 3. 批量导入附加帧（按 frame_001 对应）

**做什么**：多张附加图按文件名序号追加到对应显示帧，默认居中；已有附加图不覆盖。

**怎么做**：
- 工具栏「批量附加帧」多选文件；胶片条空白处或帧卡片上拖入多张图
- `parseFrameSequenceNumber`：`frame_001` → 显示帧 #1（index 0）
- `bindFrameImageAttachmentsByFilename`：上传后 `layer:"above"`、transform 居中、`layerOrder` 追加
- 单张拖到某帧卡片：仍绑定到该帧（原行为）

### 4. Lite 攻击拖尾重开丢失修复

**做什么**：修复 Lite 已保存拖尾重开后看不见的问题。

**原因**：
- Lite 会持久化空的 `frameSlices: {}`，绘制时当成“按帧模式”但无启用切片 → 不画
- 内置贴图路径 `tools/animation_tuner/public/presets/...` 在 `XSXB_LITE_ROOT` 下 404

**怎么做**：
- 空 `frameSlices` 视为无切片；加载时若 Lite 且有棍子则按棍子帧自动补默认切片
- Lite `/asset`：先查 Lite 根，再回退到工具根

### 5. Lite 导出包反向导入

**做什么**：把已经导出的 Sheet + JSON 或 PNG 序列整包导回 Lite 继续改，不用再分别点 PNG 和 JSON。

**怎么做**：
- 导入来源增加「Lite 导出包（反向导入）」；用目录选择器选批次根目录（含各动作的 `spritesheet.png`/`spritesheet.json` 或序列 `export.json`，以及共享 `audio/`）
- 识别 `lite-export.json`；音频可走内存 payload；烘焙包会清该动作的组/帧变换，避免叠两次偏移
- 导出补写批次级 `lite-export.json` 以及 `meta.profileId/animationId/frameRate/baked`

### 6. 胶片条重排 / 删帧 / 空白帧

**做什么**：Full 和 Lite 都可以调整序列里每张图的顺序、删掉其中一帧、在右侧插入空白透明帧。Codex 宠物不提供这些操作。

**怎么做**：
- 卡片上方 `⋮⋮` 手柄拖到另一张的左/右侧空隙；不要复用附件图层的竖向拖动，也不要拖主缩略图排帧
- 卡片右上角 `×` 删除，先确认，至少留 1 帧；键盘 Delete 仍只用于删盒子
- 时长旁 `+` 插入空白透明 PNG，尺寸和时长跟邻帧，不复制音效/附件/拖尾
- 共享模块 `tools/frame_sequence_edit.js`，接口 `POST /api/edit-frames`

### 7. 打开 / 新建项目

**做什么**：完整版打开 Godot/Unity 工程根；Lite 打开当前根下未登记的素材项目，或新建一个。

**怎么做**：
- 侧栏「打开项目」走本机 `GET /api/fs/list` 文件夹浏览器（浏览器拿不到 `D:\...` 绝对路径）
- 完整版确认时校验 `project.godot` 或 `Assets`+`ProjectSettings`，已有绑定则激活，否则 `POST /api/projects`
- Lite「新建项目」`POST /api/projects` 带 label；打开时 `action: discover` 扫描 `data/lite/projects/<id>`
- Codex 宠物不提供打开游戏工程

### 8. Photopea 编辑本帧

**做什么**：当前帧主图 + 附加层送进 Photopea，按层写回源 PNG。像素级调色/橡皮擦不进 Base 面板。

**怎么做**：
- `tools/animation_tuner/public/photopea_bridge.js` 用 ArrayBuffer `postMessage`（不能塞 `http://127.0.0.1/.../asset` URL）
- 写回 `POST /api/replace-frame`，首次覆盖前备份 `*.xsxb-backup.png`
- 只改源像素；Tuner JSON 缩放/偏移不叠第二次
- 图集裁切帧和 Codex 宠物图集不写回

### 9. 组合时间线（序列帧 NLE）

**做什么**：把多条序列（尤其是特效）叠在同一毫秒时间线上预览，导出时烘焙成一条普通 PNG。交互对照 OpenCut classic（独立缩放、磁铁、分割、拖动不整表重绘），不嵌入 React OpenCut。

**怎么做**：
- 数据：`composite_sequence.js` 的 `clips` / `tracks` / `splitClip` / `bakeTimeline`；组合组 `kind: composite`
- 视图：`composite_timeline.js`（工具条、标尺、播放头、Ctrl+滚轮缩放、磁铁指示线、边缘自动滚动）
- 大纲「基于此素材新建组合序列 / 新建组合序列」；Lite 导入方式「新建组合序列（空时间线）」；「加入时间线」可跨素材集
- 加入一条序列默认新建一层；同一轨可并排放不重叠 clip；拖到最后一行下面新建图层
- S 或剪刀在播放头按源帧边界切开；隐藏轨/clip 不进预览和烘焙
- Codex 宠物隐藏组合 UI；Godot/Unity 只播烘焙序列，不消费 clip 列表

---
